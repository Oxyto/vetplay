# Drizzle
# Replace with your DB credentials!
DATABASE_URL="postgres://user:password@host:port/db-name"

# Google Gemini client-side demo key
# Prototype only: move this behind a backend/serverless function in production.
PUBLIC_GEMINI_API_KEY=""
